/**
 * Service layer for domain orchestration.
 *
 * <p>This package is intentionally reserved to host the Gemini Vision OCR pipeline
 * with strict validation stages and defensive exception handling for malformed JSON
 * or extraction failures.
 */
package com.truew8.service;
